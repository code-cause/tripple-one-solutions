// contional statements are used to perform different actions based on different conditions. In JavaScript, the most common conditional statements are `if`, `else if`, and `else`.
// if
// if else
// if else if else
// switch
let day=2;

switch(day){
    case 1:{
        console.log('monday');
        break;
    }
    case 2:{
        console.log('tuesday');
        break;
    }
}

    
